import { Card } from "@vkontakte/vkui";

interface Props {
  //TODO
  aaa: string;
}

export const EmployeeCardBodyMatrix = ({
  props: { aaa },
}: {
  props: Props;
}) => {
  return <Card>Matrix here</Card>;
};
